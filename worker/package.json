{
  "name": "school-docs-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute school-docs --file=./schema.sql --remote"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
